server {
    listen 80;
    server_name localhost;

    location /alcohol_craving/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /alcohol_craving/index.html;
    }

    # Redirect root to subpath
    location = / {
        return 301 /alcohol_craving/;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
